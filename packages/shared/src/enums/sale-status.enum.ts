/**
 * Sale Status Enum
 * Represents the lifecycle status of a sale transaction.
 */
export enum SaleStatus {
    DRAFT = 'draft',
    COMPLETED = 'completed',
    ANNULLED = 'annulled',
    REFUNDED = 'refunded',
}
